import { Messages } from "./components/Messages";

function App() {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        marginTop: "6rem",
      }}
    >
      <Messages />
    </div>
  );
}

export default App;
